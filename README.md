## Instructions for installing on Raspberry Pi

```
sudo apt-get install libhidapi-dev -y
cd /usr/local/src
sudo git clone https://github.com/edorfaus/TEMPered.git
cd TEMPered/
sudo cmake .
sudo make
```
